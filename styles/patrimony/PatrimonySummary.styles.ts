import { StyleSheet } from 'react-native';

export const patrimonySummaryStyles = StyleSheet.create({
  gradient: {
    width: '100%',
    paddingVertical: 32,
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 24,
  },
}); 