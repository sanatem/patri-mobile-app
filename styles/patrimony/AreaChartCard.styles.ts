import { StyleSheet } from 'react-native';

export const areaChartCardStyles = StyleSheet.create({
  card: {
    backgroundColor: '#fff',
    borderRadius: 32,
    padding: 28,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.10,
    shadowRadius: 24,
    elevation: 10,
    marginHorizontal: 16,
    marginTop: 0,
    marginBottom: 32,
    width: 'auto',
    minWidth: 0,
  },
}); 