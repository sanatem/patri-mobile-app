import { StyleSheet } from 'react-native';

export const tipBoxStyles = StyleSheet.create({
  container: {
    backgroundColor: 'rgba(255, 86, 3, 0.1)',
    borderColor: 'rgba(255, 86, 3, 0.1)',
    borderWidth: 1,
  },
}); 